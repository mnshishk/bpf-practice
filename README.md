# bpf-practice

run with sudo python3 filename.py

-- flowTracking.py --
    in the terminal, run the command "sudo python3 flowTracking.py". Then, open the terminal and run a command to generate network traffic. An example command is: "wget https://google.com"